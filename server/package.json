{
  "name": "server-side",
  "version": "1.0.0",
  "description": "web and api server, serve out bundled client stuff and also serves out game logic for multiplayer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "cd ../client && npm run prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "firebase-admin": "^6.0.0"
  }
}
